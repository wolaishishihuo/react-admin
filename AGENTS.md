# AGENTS.md

本文件是仓库级 Agent 执行入口。

## 指令优先级

- 当前任务的明确要求 > 距目标文件最近的 `AGENTS.md` > 仓库根 `AGENTS.md` > 本文件路由的专题文档。低优先级规则冲突时不适用。
- 用户明确要求改变既有行为时，同步修改实现、测试和对应文档。其他情况下若文档与实现不一致，结合用户目标、测试和 Git 历史判断；不能默认判定代码或文档天然正确，仍不明确时先询问。

## 指令路由

- 所有代码实现与代码审查：读取 `CLAUDE.md` 和 `docs/ARCHITECTURE.md`。
- 路由、文件路由、`staticData`、菜单授权或加页面：继续读取 `docs/ROUTING.md`。
- 表格、列表查询或 ProTable：继续读取 `docs/PROTABLE.md`。
- UI、布局、主题或样式：继续读取 `docs/DESIGN.md`。
- 纯文档任务：读取目标文档及其直接引用来源，无需加载无关专业文档。

## 工作规则

- 实现前明确假设、歧义、取舍和可验证的成功标准；无法安全判断时先询问。
- 使用解决问题所需的最少代码。单次使用的逻辑默认内联，不提前创建配置层、通用 hook 或 DSL。
- 只修改与目标直接相关的代码；不顺带重构、改格式或清理既有死代码。
- 遵循现有目录、命名、导入和组件风格。删除仅因本次改动产生的无用代码。
- 先用测试或可复现检查定义目标，完成后按风险循环验证，不能以“看起来可用”代替结果。

## 验证

- 纯文档改动：`pnpm exec prettier --check AGENTS.md CLAUDE.md README.md docs/*.md` 与 `git diff --check`。
- 普通代码改动：至少运行 `pnpm type:check`、`pnpm lint:check`、`pnpm format:check` 与相关测试。
- 路由、认证、HTTP、Tabs、缓存或发布前验证：运行 `pnpm verify`。
- 交付前检查 `git status --short` 和最终 diff，确认没有无关改动或生成物漂移。
- 不自动提交或推送；只有用户明确要求时才执行 Git 写操作。

## 代码审查

- 问题优先，按行为错误、安全问题、回归风险和缺失测试从高到低排列，并附文件与行号。
- 重点检查认证/session 隔离、路由 identity、HTTP 重放、Query 缓存、Tabs 与页面缓存契约。
- 不把 Prettier、ESLint 等自动化工具能够直接报告的问题当作主要人工审查结论。
- 没有发现问题时明确说明，并列出尚未运行的验证或残余风险。
