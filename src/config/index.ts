// 全局默认配置

// 首页地址（与 mock 菜单 element 对应）
export const HOME_URL: string = '/home/index';

// 登录页地址
export const LOGIN_URL: string = '/login';

// 默认主题色
export const DEFAULT_PRIMARY: string = '#1677ff';

// 路由白名单（错误页未登录可直达）
export const ROUTER_WHITE_LIST: string[] = ['/403', '/404', '/500'];
